# Project Documentation

The repository is organized under `app/` by routes, persistence models, service pipeline, templates, and static UI. Start with `README.md`, use `ARCHITECTURE.md` for engineering detail, `SECURITY.md` for trust boundaries, and `USER_GUIDE.md` for operational use.

